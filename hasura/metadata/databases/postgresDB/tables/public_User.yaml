table:
  name: User
  schema: public
select_permissions:
  - role: customer
    permission:
      columns:
        - created_at
        - email
        - id
        - role
        - updated_at
      filter:
        id:
          _eq: X-Hasura-User-Id
    comment: ""
